export enum PreviewModes {
  Desktop = 'lg',
  Tablet = 'md',
  Mobile = 'sm',
}

export enum ModeSizes {
  Desktop = 1026,
  Tablet = 720,
  Mobile = 320,
}

export const controlContainerWidth = 240
